
export const enum PaginationType {
    server,
    client
}

export const enum TableChange {
    init,
    search,
    pageNumber,
    pageSize,
    sorting
}

export const enum SortChangedType {
    init,
    column,
    cancel,
    clearAll,
    option
}

export const enum SortingTypeLocation {
    commandPanel,
    optionPanel    
}

